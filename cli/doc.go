// Package cli is the kno binary, built on fang over cobra.
//
// The bar: a developer should never wonder what just happened, what it cost,
// or what to do next. Every command answers all three. Cost is always visible
// and confirmed before it is spent, every command ends by naming the next one,
// interruption is boring (checkpoint, then --resume), and errors name the exact
// fix.
package cli
